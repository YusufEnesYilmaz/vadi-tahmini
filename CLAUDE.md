# Vadi Tahmini

Arkadaş grubuyla oynamak için LoLdle tarzı League of Legends tahmin oyunu.
Slogan: "Bil bakalım, şampiyon kim?"
Ticari değil; Riot "Legal Jibber Jabber" fan politikası kapsamında. LoLdle adı kullanılmaz.
Konum: `C:\Users\yusfe\Desktop\Claude\vadi-tahmini`. localStorage önekleri: `vt:`.

## Mimari Özeti

- **Vite + React + TS + Tailwind v4 + vite-plugin-pwa** — saf client-side SPA, backend yok.
- **Menü hiyerarşisi**: üst mod (Sınırsız / Günlük / Zamana Karşı) → alt mod (Klasik / Yetenek / Görsel / Kostüm). 12 kombinasyon.
- Veri: `scripts/build-data.mjs` → `src/data/champions.json` (ddragon tr_TR + Meraki). Görseller repo'da YOK, runtime'da ddragon CDN'den gelir.
- Deploy hedefi: Vercel + PWA. GitHub/Vercel bağlantısını kullanıcı kendisi yapar; **git komutu çalıştırma** (kullanıcının açık isteği).

## Karar Günlüğü

| Tarih | Karar | Gerekçe |
|---|---|---|
| 2026-07-20 | Vite, Next.js değil | SSR/route gereksiz; kullanıcı overengineering istemiyor |
| 2026-07-20 | Rastgelelik: `crypto.getRandomValues` + mod başına deste (Fisher-Yates, localStorage persist, reshuffle'da son≠ilk garantisi) | Kullanıcının açık isteği: iki kişi oyunu açınca aynı şampiyon GELMEMELİ; art arda tekrar da olmamalı. Tek istisna Günlük mod: `fnv1a(tarih+mod)` — bilerek herkese aynı (skor karşılaştırma) |
| 2026-07-20 | Zamana Karşı, Sınırsız ile AYNI desteden çeker | İki modda da tekrar hissi olmasın |
| 2026-07-20 | Bölge+yıl+koridor Meraki'den, cinsiyet script içi statik tablo | ddragon'da yok; Meraki'de gender yok. Yeni şampiyonda Meraki gecikirse `NEW_CHAMP_FALLBACK` devreye girer, rapor uyarır |
| 2026-07-20 | Kroma filtresi: `name.trim().endsWith(')')` | ddragon skins listesinde kromalar parantezli ("(Yakut)") ve splash'ları CDN'de 403; bazı adların sonunda boşluk var, trim şart |
| 2026-07-20 | Skin havuzu skin bazlı deste (~1917), base skin hariç | Kostüm modu şampiyon değil kostüm tahmini |
| 2026-07-20 | Güncelleme: Ayarlar'da 2 katman — SW prompt (`registerType:'prompt'`) + ddragon veri çekme (localStorage'a, gömülü JSON yedek kalır) | Kullanıcının isteği: push'suz güncelleme; yeni şampiyon/kostüm oyuncu tarafından çekilebilsin |
| 2026-07-20 | Classic kolonları: Cinsiyet, Rol, Koridor, Kaynak, Menzil, Bölge, Yıl | Koridor kullanıcı isteğiyle eklendi (LoLdle paritesi) |
| 2026-07-20 | **Klavye kısayolları:** tuş bonusunda 1-5 ve P/Q/W/E/R, tur bitince Enter = Sonraki / Tekrar | Masaüstünde fareye uzanmadan oynanabilsin. Yazı alanı odaktayken kısayollar devre dışı — şampiyon adı yazarken "q" harfi tuş seçmemeli |
| 2026-07-20 | **Erişilebilirlik:** `aria-live` bölgesi tahmin sonucunu duyuruyor | Görme engelli oyuncu için tahmin sonucu yalnız renkle veriliyordu. Duyuru metni sonucu + kalan hakkı söylüyor ("Teemo yanlış. 4 hakkın kaldı.") |
| 2026-07-20 | **Ses efektleri** WebAudio ile üretiliyor, hazır ses dosyası YOK (`sfx.ts`) | Ses dosyası indirmek PWA önbelleğini şişirirdi; osilatörle üretilen kısa notalar 0 bayt ve çevrimdışı çalışıyor. Ses seviyesi Replik modundaki kaydırıcıyla ORTAK (`vt:volume`), açma/kapama Ayarlar'da (`vt:sfx`) |
| 2026-07-20 | **Görsel paylaşım kartı** (`shareCard.ts`, canvas → PNG) | Emoji ızgarası cihazdan cihaza kayıyordu (emoji genişlikleri farklı). Kart canvas'ta çiziliyor, kütüphane yok. `navigator.share` destekliyorsa sistem paylaşımı, yoksa indirme. Metin paylaşımı KALDIRILMADI — yan yana iki buton ("Metin" / "🖼 Görsel") |
| 2026-07-20 | **Hata sınırı** (`ErrorBoundary.tsx`, `main.tsx`'te App'i sarar) | Replik modundaki çökmede ekran bomboş kaldı, kullanıcı ne olduğunu anlamadı. Artık "bir şeyler ters gitti" ekranı + Tekrar dene / Sayfayı yenile + açılır teknik ayrıntı (kullanıcı ekran görüntüsüyle iletebilsin). Sınıf bileşeni olmak ZORUNDA — React hata yakalamayı hook'larda desteklemiyor |
| 2026-07-20 | **Otomatik testler** (vitest, `npm test`) — 6 dosya / 36 test | Deste tekrarsızlığı, günlük determinizm, zorluk kuralları, klasik değerlendirme, istatistik kayıtları elle test ediliyordu; sessizce bozulacak türden şeyler. **jsdom KURULMADI** — mantık testleri tarayıcı istemiyor, `src/test/setup.ts` localStorage'ı bellekte taklit ediyor. `puzzle.test.ts` yeni şampiyon gelip emojisi yazılmayınca kırmızı yanar (bakım kırılganlığına karşı erken uyarı) |
| 2026-07-20 | **Tasarım sistemi düzeltmesi** (kod taramasıyla tespit, Hallmark skill'i KURULMADI) | Kullanıcı [usehallmark.com](https://www.usehallmark.com/) skill'ini önerdi; incelendi (MIT, markdown, landing-page odaklı) ve **gerekmediğine karar verildi** — mevcut oyun arayüzünün oturmuş bir dili var, Build kipi onu bozardı; kurulsa her projeye otomatik eklenirdi. Yerine elle denetim yapıldı |
| 2026-07-20 | Tipografi ölçeği Tailwind adımlarına indirildi (12/14/16/18/20/24/30) | 9px, 10px, 11px keyfi boyutları vardı — 9px telefonda okunmuyordu, aralarında sistem yoktu |
| 2026-07-20 | Yarıçaplar üçe indi: `md` hücre/rozet, `xl` buton/kart, `2xl` pencere | Altı farklı yarıçap vardı, hangi elemanın hangisini aldığı tesadüfiydi |
| 2026-07-20 | Ham renkler değişkene taşındı: `--danger-text`, `--overlay`, `--gold-soft`, `--on-gold` | `#f87171`, `rgba(4,7,15,.75)`, `#0a0e1a` bileşenlere dağılmıştı; tema değişse geride kalırlardı |
| 2026-07-20 | Başlık fontu: **sistem serifleri** (`--font-display`: Georgia/Palatino), gövde sans kalıyor | Arayüzün tipografik kimliği yoktu, her şey sistem sans'tı. LoL'ün başlık dili serif (Beaufort). **Dışarıdan font ÇEKMİYORUZ** — PWA çevrimdışı çalışmalı, CDN yok; gömülü font ~25 KB getirirdi. Sistem serifi 0 bayt |
| 2026-07-20 | Hiyerarşi: asıl içerik `--text-dim`'den `--text`'e çıkarıldı | Metnin %85'i soluk gridi (78 dim / 12 text), hiçbir şey öne çıkmıyordu. Artık yalnız yardımcı metinler soluk |
| 2026-07-20 | Masaüstü: menü `max-w-2xl` + modlar ızgarada, oyun ekranı `max-w-3xl` | 1280px'de içerik ortada dar bir şeritti, ekranın üçte ikisi boştu |
| 2026-07-20 | **Hata düzeltmesi:** Zamana Karşı hiç `recordGame` çağırmıyordu → istatistik "henüz oynanmadı" diyordu | Sadece rekor skor yazılıyordu, tur sayacı hiç artmıyordu. Ayrı bir `recordTimedRun(sub, diff, score)` yazıldı (`recordGame`'in "kazandın mı / kaç tahminde" sözlüğü Zamana Karşı'ya uymuyor); `played` ve yeni `totalScore` alanını artırıyor, ort. skor buradan geliyor. **`recordedRef` guard'ı şart** — StrictMode efektleri iki kez çalıştırdığı için tur çift sayılıyordu |
| 2026-07-20 | Zamana Karşı'ya **Pas'sız seri** (combo): Pas'a basınca sıfırlanır, tur rekoru `vt:combo:{sub}:{diff}`'e yazılır | Kullanıcının fikri. Skor tek başına "kaç Pas geçtiğini" gizliyordu; seri, Pas'a sığınmadan üst üste kaç bildiğini ölçüyor. Yanlış tahmin seriyi BOZMAZ (zaten doğruyu bulana kadar deniyorsun), yalnızca Pas bozar. Yetenek modunda tuş bonusunu kaçırmak seriyi bozmaz — şampiyonu bilmek yeterli |
| 2026-07-20 | **Tahmin hakkı sınırı** (Kolay 10 / Normal 8 / Zor 6 / Aşırı 5) — Sınırsız ve Günlük'te | Kullanıcının tespiti: kaybetmek imkânsızken "kazanma oranı" istatistiği hep %100 çıkıyordu, ölçtüğü bir şey yoktu. Hak bitince cevap açıklanır, oyun kayıp kaydedilir (`recordGame(..., false, ...)`), seriler sıfırlanır. **Zamana Karşı'da uygulanmaz** — orada baskıyı süre kuruyor, Pas zaten var. Kayıp kaydı olay işleyicisinde (efekte değil) — StrictMode'da çift sayılmasın |
| 2026-07-20 | İstatistik derinleştirme: **tahmin dağılımı** (1/2/3/4/5/6+ kutuları), **tek seferde bilme** sayacı ve **tek seferde serisi** | Kullanıcı isteği. `ModeStats`'a `firstTry`, `firstTryStreak`, `bestFirstTryStreak`, `dist` eklendi. Son kutu "6+" — uzun kuyruk tek yerde toplanıyor, yoksa grafik saçaklanıyor. `getStats` eski kayıtlarda eksik/kısa `dist`'i normalize eder (göç kodu gerekmeden eski veri okunabilir) |
| 2026-07-20 | **Günlük takvim** (`DailyCalendar.tsx`): hangi gün kaç mod çözüldü, ay ay gezilir | Kullanıcı isteği. Yeni depo: `vt:dhistory` = `{ tarih: { mod: tahminSayısı } }`, `saveDailyState` kazanınca yazar. Eskiden yalnız BUGÜNÜN durumu tutuluyordu, geçmiş kayboluyordu. Renk doygunluğu = o gün bitirilen mod oranı. Pazartesi haftanın ilk günü (TR takvimi) |
| 2026-07-20 | Zorluk karşılaştırma tablosu (`DifficultyTable.tsx`) — menüdeki şeridin altında açılır/kapanır + "Nasıl oynanır" içinde | Kullanıcı "zorlukları anlatan bir yer" istedi. **Satırlar `RULES`'tan türetiliyor, elle yazılmıyor** — `difficulty.ts`'te bir eşik değişince açıklama kendiliğinden güncelleniyor, ikisi çelişemiyor. "yok/açılmaz" değerleri kırmızıya boyanıyor |
| 2026-07-20 | **İstatistik ekranı** (`Stats.tsx`, menüden 📊) | `recordGame`/`recordScore` en baştan veri topluyordu ama tek görünen şey üst çubuktaki "Seri" idi. Üst mod + zorluk sekmeleriyle 18 kombinasyon gezilebiliyor; özet (oynanan / kazanma % / en iyi seri) + mod mod döküm |
| 2026-07-20 | **Günlük panel** (`DailyPanel.tsx`): gece yarısına geri sayım, bugün biten modlar, gün serisi, "Günü paylaş" | Günlük modun tek geri bildirimi "Yarın yeni bulmaca seni bekliyor" cümlesiydi. Gün serisi mod bazlı DEĞİL genel (`vt:dstreak`): üst üste kaç gün oynandığını sayar, gün atlanırsa sıfırlanır. Tarih hesabı öğlen saatinden yapılır ki yaz saati kaymaları günü kaydırmasın |
| 2026-07-20 | Tek paylaşım kartı: günün tüm modları tek metinde | Altı modu ayrı ayrı paylaşmak grup sohbetini boğuyordu |
| 2026-07-20 | Görsel modu artık kostüm splash'larını da kullanıyor (`Puzzle.splashNum`) | Aynı şampiyon tekrar geldiğinde aynı görsel çıkıyordu. **%50 temel görsel ağırlığı var**: bazı kostümler şampiyonu tanınmaz kılıyor (Ay Kızı Diana ≠ Kan Ayı Diana), hepsi kostüm olsa mod adaletsizleşirdi. Günlük'te seçim tarihten türeyen rng ile — herkeste aynı görsel |
| 2026-07-20 | **Zorluk seviyeleri** (Kolay/Normal/Zor/Aşırı Zor) — kullanıcının fikri; "ipuçlarını dengeleme" işiyle birleştirildi | İpucu takvimi zaten zorluğun kendisiydi, iki ayrı iş yapmak yerine tek sistem. Kurallar **`src/game/difficulty.ts`'te TEK KAYNAK** (`RULES`): emoji açılma, zoom, yetenek/kostüm ipucu zamanı, replik 2. klip, klasik yıl oku + kısmi renk, Zamana Karşı süresi. Bileşenler kural okur, kendi eşiklerini tutmaz |
| 2026-07-20 | Günlük'te zorluk YOK (hep normal) | Herkesin aynı bulmacayı aynı ipuçlarıyla çözmesi gerek, yoksa paylaşılan skor anlamsızlaşır |
| 2026-07-20 | Zamana Karşı süresi zorluğa bağlı: 90/60/45/30 sn | Kullanıcı tercihi. Skorlar seviyeler arası kıyaslanamaz hale geliyor, bu yüzden **rekorlar ve istatistikler zorluk başına ayrı anahtarlarda** (`vt:best:{sub}:{diff}`, `vt:stats:{top}:{sub}:{diff}`). Eski anahtarlar terk edildi — yayına çıkmadığı için göç yazılmadı |
| 2026-07-20 | Zorluk seçimi alt mod ekranında şerit; `vt:difficulty`'de hatırlanır | Kullanıcı tercihi (Ayarlar'a gömmek yerine). Oyun içinde değiştirilemiyor — tur ortasında zorluk düşürüp "hile" yapılmasın |
| 2026-07-20 | **Replik modu** (6. alt mod): CommunityDragon'ın **Türkçe** seslendirme kliplerinden şampiyonu bulma | Faz 2'nin ikinci maddesi. Yazılı replik veritabanı yok (ddragon'da da, CDragon'da da) — ama CDragon'da hazır ses var. **Kapsam ölçüldü: 173/173 şampiyon için hem `champion-ban-vo` hem `champion-choose-vo` `tr_tr` altında mevcut** (2026-07-20). Yeni veri dosyası GEREKMEDİ: URL şampiyonun sayısal `key`'inden türüyor |
| 2026-07-20 | Replikte sıra: önce **yasaklanma** klibi, 2 yanlıştan sonra **seçilme** klibi | Seçilme repliği çok tanıdık (çoğu oyuncu ezbere bilir), yasaklanma repliği daha kapalı. Kolay olan ipucu olarak sonraya saklandı |
| 2026-07-20 | Ses klipleri servis çalışanında `CacheFirst` (`cdragon-vo`, `rangeRequests: true`) | ddragon görselleriyle aynı mantık; dinlenen klip tekrar inmesin. `rangeRequests` ses için şart — tarayıcı kısmi istek atıyor |
| 2026-07-20 | **Emoji modu** (5. alt mod): veri `src/data/emoji.json`, şampiyon başına 4–5 emoji, sıra belirsizden belirgine | Faz 2'nin ilk maddesi. ddragon'da böyle bir veri yok, elle yazıldı. Açılma: **ilk emoji açık, her yanlışta bir tane daha** (kullanıcının isteği — başta 2 açıktı, çok kolaydı). Kilitli olanlar `❔` kutusu olarak görünür ki oyuncu kaç ipucu kaldığını bilsin |
| 2026-07-20 | Emoji havuzu yalnızca verisi olan şampiyonlardan çeker (`EMOJI_IDS`) | Yeni şampiyon geldiğinde emojisi yazılana kadar moda girmez — boş ipuçlu, çözülemez bulmaca göstermektense atlamak daha iyi. Locke/Yunara/Zaahen kullanıcıdan alınan tariflerle dolduruldu, artık 173/173 |
| 2026-07-20 | Emoji düzenleme aracı: `node scripts/emoji-review.mjs` → `tools/emoji-review.html` (git'e girmez) | Uygulamaya dev-only ekran gömmek yerine tek seferlik üretilen statik HTML: veri gömülü geldiği için `file://` fetch sorunu yok, kullanıcı düzenleyip "JSON'u indir" ile `src/data/emoji.json`'ı değiştiriyor. Emoji bölme `Intl.Segmenter` ile grafem bazlı — bayrak/ZWJ'li emojiler bozulmuyor |
| 2026-07-20 | Klasik tablosunda "yanlış" rengi kırmızıdan (#7f1d1d) nötr gri-maviye (#3b4455) çekildi; kehribar `--partial` altın temaya yaklaştırıldı; gerçek uyarı için ayrı `--danger` eklendi | Wordle/Wordi referanslarında "yok" nötr gridir — her satırın çoğu hücre yanlış olduğu için kırmızı tablo alarm panosuna dönüyor, doğru/kısmi hücreler seçilemiyordu. Süre sayacı artık `--danger` kullanıyor ki kırmızı gerçekten "dikkat" demeye devam etsin |
| 2026-07-20 | **Nasıl oynanır** penceresi (`HowTo.tsx`) — menüden ve oyun içi "?" butonundan | Wordi referansındaki how-to-play ekranı. Renklerin anlamı (yeşil/kehribar/gri) hiçbir yerde yazmıyordu; arkadaş grubuna link atıldığında ilk soru bu olacaktı. Oyun içinden açılınca sadece o modun anlatımı gösterilir |
| 2026-07-20 | UI cilası: ortak `.card-btn` (hover/active) + `anim-row/pop/shake/pulse` sınıfları `index.css`'te toplandı | Stiller inline `style` ile yazıldığı için hover/focus yazılamıyordu; tek yerde CSS sınıfı olarak durunca bileşenler kalabalıklaşmıyor. `prefers-reduced-motion` ile hepsi kapanıyor, `:focus-visible` altın halka klavye kullanıcısı için |
| 2026-07-20 | Mobilde Classic tablosunun altına "← tabloyu yana kaydır →" ipucu | 375px'te tablo (560px) kaydırılabilir ama görsel bir işaret yoktu; oyuncular Yıl/Bölge kolonlarını görmeden tahmin ediyordu. `sm:hidden` — masaüstünde çıkmaz |
| 2026-07-20 | Ad **"Vadi Tahmini"** (slogan: "Bil bakalım, şampiyon kim?") | Kullanıcı "Bil Bakalım Şampiyon" ile arasında kalmıştı; 12 karakter olduğu için PWA `short_name`'e ve tek satır menü başlığına sığıyor, paylaşım metni de kısa kalıyor — uzun aday slogana çevrildi. "Vadi" = Summoner's Rift, marka adı geçmiyor. Değişen yerler: `index.html`, `vite.config.ts` (manifest), `Menu.tsx`, `Settings.tsx`, `share.ts`, `package.json`, README. Kullanıcının isteğiyle klasör adı da `lol-tahmin` → `vadi-tahmini` ve localStorage öneki `lt:` → `vt:` taşındı; ikon takımı (favicon + PWA) altın "V" tasarımına geçti. Önek değişimi eski kayıtları görünmez kılar — yayına çıkmadan yapıldığı için sadece test verisi etkilendi, göç kodu yazılmadı |
| 2026-07-20 | Classic'ten **Rol kolonu kaldırıldı** (7 kolon: Cinsiyet, Koridor, Kaynak, Menzil, Bölge, Yıl) | Kullanıcı: rol ile koridor çakışıyor, kafa karıştırıyor. Veri katmanında `roles` DURUYOR (`types.ts`, `dataUpdate.ts`, `champions.json`) — geri eklemek istenirse tek yer `ClassicBoard`+`classic.ts`+`share.ts`. Paylaşım grid'i de 6 kareye düştü |
| 2026-07-20 | Yetenek modunda **tuş bonusu**: şampiyon bilinince "Peki bu hangi tuş?" (Pasif/Q/W/E/R, tek hak) | Kullanıcının isteği (LoLdle paritesi). Tuş artık 3 yanlışta ipucu olarak VERİLMEZ — cevabı peşkin verirdi; onun yerine 3 yanlışta yetenek adı açılır. Zamana Karşı'da doğru tuş +1 puan (şampiyon 1 + tuş 1), diğer modlarda sadece gösterilir. Günlük'te seçilen tuş `DailyState.slot`'a yazılır (yenilemede kaybolmasın) ve paylaşım metnine 🟩/🟥 satırı eklenir |
| 2026-07-20 | **İki düzeltme (Zamana Karşı × tuş bonusu kesişimi):** (1) Enter kısayolu süre akarken de "Sonraki"yi tetikler; (2) süre tuş sorusu beklerken dolarsa şampiyonun +1'i ve serisi yanmaz (kayıt efektinde `pending` telafisi) | (1) Kısayol `!timed` koşuluyla yazılmıştı — tam da saniyenin değerli olduğu modda fare zorunluydu. (2) Skor `handleSlot`'ta yazıldığı için tuş seçilemeden süre bitince doğru bilinen şampiyon kayda girmiyordu; seri kuralı "şampiyonu bilmek yeter" olduğundan seri de +1 alır |

| 2026-07-20 | **İki ayrı gün serisi** (kullanıcı "A: iki ayrı seri" seçeneğini seçti): gevşek 🔥 gün serisi (en az 1 mod tamamlamak yeter, mevcut) + prestij ⭐ **Tam Gün serisi** (`vt:dfullstreak`, 6/6 mod tamamlanınca, kazanmak şart değil). İkisi de DailyPanel'de üst üste ve Stats günlük sekmesinde iki satır gösteriliyor. DailyPanel çipleri artık üç durumlu: ✓ kazanıldı / ✗ kaybedildi / ○ oynanmadı; paylaşım metninde kayıp mod "✗" | "Tam gün oynama ile tek mod oynama arasında fark olsun mu" sorusuna kullanıcının cevabı. Tam Gün koşulu `saveDailyState` içinde 6 modun `done` durumuna bakılarak anlık hesaplanıyor — ayrı gün-gün tam gün kaydı TUTULMUYOR, seri kendi anahtarında; günde bir kez sayılır (idempotent) |

| 2026-07-20 | **Gün serisi artık tamamlamayla ilerliyor** (`saveDailyState`: `s.done` yeterli, kazanmak şart değil) — eskiden yalnız kazanılan gün sayılıyordu | Kullanıcı kararı: "günlüğü tamamlaması yeterli, kaybetmesi kazanması önemli değil". Rozet açıklamalarıyla da uyumlu hale geldi ("üst üste günlük OYNA"). Aynı gün ikinci tamamlama sayılmaz (gün başına bir artış). Aynı oturumda kullanıcının onayladığı diğer notlar: mix kazançları gerçek mod sayaçlarına İŞLENMEZ (öyle kalacak); Zamana Karşı'da kazanma kavramı yok (öyle kalacak); meydan okuma turlarının istatistiğe yazılması düşünülüyor (şimdilik yazıyor) |

| 2026-07-20 | **Kostüm modu arama düzeltmesi:** otomatik tamamlama artık şampiyon adını da arıyor (`sub` alanı) + sonuç üst sınırı kostüm modunda 20 (`maxResults` propu) | Arama yalnız kostüm adına bakıyor ve 8 sonuçla kesiyordu — "nami" yazınca Nami'nin ~10 kostümünün bir kısmı hiç görünmüyordu. Sıralama: etiket başlangıcı > alt yazı başlangıcı > içerme |
| 2026-07-20 | Kırpma odağı %35–65'e daraltma denemesi **geri alındı** — kullanıcı sorunu kendisi farklı çözecek | %20–80 rastgele odak splash'teki başka karaktere denk gelebiliyor (cevap Karthus, parçada yordle). Merkez bandı çözümü kullanıcı istemedi; kod eski halinde, çözüm kullanıcıda |
| 2026-07-20 | **`DAILY_OVERRIDES`** (`puzzle.ts`): tarih anahtarlı elle günlük sabitleme — ilk giriş `2026-07-20` görsel = Kızıl Garen (id Garen, splashNum 1) | Kullanıcı isteği. Tarih geçince giriş kendiliğinden etkisiz. UYARI: SW güncellemesini almamış oyuncu o günün ESKİ bulmacasını görür — override günü herkese güncellemeyi kabul ettirmek gerek. Test koşullu (override yalnız kendi gününde aktif, sonrası test koşulsuz geçer) |

| 2026-07-20 | **İki düzeltme (yayın sonrası):** (1) Başarım vitrini açılırken `evaluateAchievements()` çalışıyor (`getAchievementShowcase` içinde) — rozet özelliğinden ÖNCE oluşmuş istatistikler silik görünüyordu, artık vitrin açılışında geriye dönük eşitleniyor. (2) Günlük kayıp da `vt:dhistory`'ye yazılıyor: değer 0 = "oynandı, kaybedildi" → takvim detayında "✗ kaybedildi". Hücre doygunluğu ve "Tam Gün" rozeti yalnız >0 (kazanç) sayar; `recordDailyLoss` kazanç kaydının üzerine yazmaz | (1) `vt:ach` deposu yalnız oyun sonu tetiklemesiyle yazıldığı için eski istatistikler hiç rozete dönüşmüyordu; vitrinin kendini eşitlemesi en temiz çözüm (tetikleme kaçsa bile doğrular). (2) Kayıp hiçbir yerde tutulmuyordu — `vt:daily:{sub}` yalnız bugünü saklıyor, geçmiş günün kaybı kayboluyordu |

| 2026-07-20 | **`vercel.json` ile güvenlik başlıkları** (yayın öncesi güvenlik taramasından çıkan tek bulgu) | Tarama temizdi (0 bağımlılık zafiyeti, XSS yüzeyi yok, `?c=` payload'ı beyaz listeyle doğrulanıyor, sır yok); tek eksik HTTP güvenlik başlıklarıydı. CSP: script/img/connect yalnız kendi origin + ddragon + meraki + cdragon'a açık; `style-src 'unsafe-inline'` ZORUNLU — bileşenler inline `style` kullanıyor; `img-src blob:` paylaşım kartı önizlemesi için. Ayrıca nosniff, frame-ancestors 'none', Referrer-Policy, Permissions-Policy (kamera/mikrofon/konum kapalı). Başlıklar yalnız Vercel'de geçerli, `vite dev`'i etkilemez |
| 2026-07-20 | **Karışık mod (mix)** — Faz 3/A: her soru rastgele başka tipten | Kullanıcı isteği. `SubMode` 6'lı KALDI (günlük/veri/emoji yolları hep gerçek tiple çalışır); yeni `PlaySub = SubMode \| 'mix'` yalnız oyun yüzeyinde (menü/GameScreen/istatistik anahtarı) yaşar, `Puzzle.sub` HER ZAMAN gerçek tip. `nextPuzzle(top,'mix')` izinli havuzdan gerçek tip seçip normal yola delege eder → her tipin kendi destesi/tekrarsızlığı korunur. Havuz: Sınırsız 6 tip (Klasik dahil), **Zamana Karşı Klasik HARİÇ 5 tip** (tablo tabanlı Klasik süre ritmini öldürüyor — kullanıcı kararı), Günlük'te mix YOK. GameScreen'de `activeSub` (=`puzzle.sub`) türetilir; `sub`'a bakan her yer (autocomplete, bonusMode, board/PuzzleView seçimi, HowTo) `activeSub` kullanır; istatistik/rekor anahtarı `sub`='mix' ile kalır (karışığın kendi tablosu). `dailySub` tip-güvenli yer tutucu (daily hiç mix olmaz). Doğrulama: unit (timed 200 örnek→classic yok, endless 300→classic var) + tarayıcı (timed 25 Pas→classic yok, 5 tip). |
| 2026-07-20 | **Meydan okuma linki (challenge)** — Faz 3/B: backend'siz, URL'e gömülü seed'le arkadaşına birebir aynı soruları gönder | Yalnız Zamana Karşı. **Determinizm kararı:** Zamana Karşı artık desteden çıkarılıp seed'li akışa (`createTimedStream`) geçti. `startTimed()` bir `runSeed` üretir; sorular, mix tip seçimi, spellIndex, crop hepsi `seededRng`'den gelir. Böylece seed'i bilen herkes birebir aynı soru dizisini alır. **Trade-off:** tur içi tekrar yok (seed'li karışım), ardışık turlar arası tekrar mümkün (173 havuzda 30-90 sn'de ~10-20 soru — düşük risk). Sınırsız'ın kalıcı deste garantisi DEĞİŞMEDİ. **Payload:** `{v,s,m,d,sc,cb,n}` → JSON → TextEncoder → base64url → `?c=...`. Türkçe karakter güvenli. `App.tsx` açılışta `?c=` okur, `history.replaceState` ile URL temizlenir. Takma ad bir kez sorulur (`vt:nick`), Ayarlar'dan değiştirilir. Sonuç ekranında iki skor yan yana + kazan/kaybet/berabere bandı. Challenge galibiyeti `vt:chwin` sayacını artırır (rozet C-15). Pas butonu `drawNext()` kullanır (determinizm bozulmasın). |
| 2026-07-20 | **Rozetler** — Faz 3/C: mevcut istatistiklerden beslenen 36 başarım (9 kategori) | `achievements.ts`: `buildSnapshot()` tüm `getStats` kombinasyonlarını, `getDailyStreak`, `getDailyHistory`, combo/best anahtarlarını, `vt:champwins`, `vt:chwin`'i tek nesnede toplar. `evaluateAchievements()` henüz kazanılmamış rozetlerin `check(snap)` fonksiyonunu çalıştırır, yeni kazanılanları `vt:ach`'a yazar. **Tetikleme:** GameScreen'de galibiyet/kayıp kaydı ve timed tur sonu efektinden sonra `setTimeout(checkAchievements, 50)` — state güncellemeleri localStorage'a commit etsin. Toast kuyruğu: sağ üstte sabit, `anim-pop`, her 2.5 sn'de birer sıra kaybolur, `playAchievement()` fanfarı (yükselen üç nota). `recordChampWin(champId)` her galibiyette çağrılır (Avcı/Koleksiyoncu/Ansiklopedi rozetleri için). Stats vitrini: grid düzeni, kazanılanlar renkli+tarih, kilitliler soluk + ilerleme çubuğu. Plan 15 rozet diyordu; kademeler (Çırak/Usta/Efsane, Avcı/Koleksiyoncu/Ansiklopedi gibi) ayrı rozetlere bölününce 36 oldu. |
| 2026-07-20 | **Rozet sayım hatası düzeltildi:** `buildSnapshot` Günlük kayıtlarını 4 kez sayıyordu ve zorluk rozetlerini yanlış açıyordu | `statsKey` Günlük'te `diff`'i yok sayıp tek anahtara yazıyor (`vt:stats:daily:{sub}`), ama snapshot `top × sub × diff` üçlü döngüsüyle okuyordu → aynı Günlük kaydı 4 zorluk için 4 kez toplanıyordu. Ölçüldü: tek Günlük galibiyetinde `totalPlayed=4, totalWon=4`. Daha kötüsü `diff === 'insane'` dalı Günlük'te de çalışıyordu → **"Gözü Kara / Zor Bela / Demir İrade" rozetleri Günlük oynayınca açılıyordu** (oysa Günlük'te zorluk seçilemez). Düzeltme: Günlük için zorluk döngüsü tek tura indirildi (`['normal']` — `rulesFor` da öyle davranıyor) + zorluk sayaçlarına `top !== 'daily'` koruması. `allSubsWon` artık `allStats`'tan türüyor (72 gereksiz localStorage okuması gitti) ve `size >= 6` yerine `SUB_MODES.every(...)` ile kesinleşti. 4 regresyon testi eklendi. |
| 2026-07-20 | Rozet kazanım tarihi yerel `todayKey()`'e geçti (eskiden `new Date().toISOString()`) | UTC+3'te gece 00:00–03:00 arası kazanılan rozet "dün" damgalanıyordu; oyunun geri kalanı (günlük mod, seri) zaten yerel `todayKey` kullanıyor — tek kaynağa hizalandı |

| 2026-07-20 | **İlerleme yedekleme** (`backup.ts` + Ayarlar): "Yedek al" JSON indirir, "Yedeği yükle" geri yükler | Her şey cihaza özel localStorage'daydı; telefon değişince 36 rozet + seriler + rekorlar giderdi. `Object.keys(localStorage)` KULLANILMADI — Storage'ın `length`/`key(i)` API'si hem tarayıcıda hem testteki bellek taklidinde çalışıyor (bu sayede test edilebildi). Yükleme **birleştirmez, yerine geçer**: iki cihazın istatistiğini birleştirmenin doğru cevabı yok (hangi seri geçerli? rekorlar toplanır mı?) — arayüz onaylatıyor. Doğrulama: `app`/`version` alanları + yalnız `vt:` önekli metin değerler alınır, yabancı içerik sızmaz; bozuk dosya mevcut veriyi BOZMAZ (önce doğrula, sonra sil). `Settings.resetProgress` de aynı `clearProgress()`'i kullanıyor. |
| 2026-07-20 | **Havuz filtreleri** (`filter.ts` + `PoolFilterPicker`): Bölge / Rol / Koridor'a göre şampiyon havuzunu daralt | Kullanıcı isteği (antrenman modu). Tek seçim; seçiliye tekrar dokunmak kaldırır. Seçenekler **veriden türetilir** (elle liste yok), her seçeneğin yanında kaç şampiyon kaldığı yazar. **Deste anahtarına filtre eklendi** (`deck:classic:region:Noxus`) — "sadece Noxus" destesi tüm havuz destesiyle karışmaz. **Günlük'te YOK** (herkes aynı bulmacayı çözmeli; `nextPuzzle` daily dalında filtre yok sayılır). **İstatistikler filtreye göre AYRILMAZ** (bilinçli): yoksa mod × zorluk × filtre yüzlerce tabloya bölünür, hiçbiri anlamlı veri toplayamaz. Havuzu boşaltan filtre (ör. o bölgede emoji verisi yok) sessizce yok sayılır — çözülemez bulmaca göstermektense tüm havuza dönmek iyi. **Meydan okuma linki filtreyi taşır** (`f` alanı) yoksa iki oyuncu farklı havuzdan oynardı; alanı olmayan ESKİ linkler `all` sayılır ve çalışmaya devam eder (test edildi). |

| 2026-07-20 | **Eşya modu** (7. alt mod) — ddragon `item.json` tr_TR → `src/data/items.json` (143 eşya) | Kullanıcı isteği. Havuz: Summoner's Rift'te satılan, **1600+ altınlık**, şampiyona özel olmayan tam eşyalar — bileşenler/iksirler/mücevherler tahmini sulandırırdı. İpuçları zorluğa bağlı (`itemGoldAt`/`itemTagsAt`/`itemPartsAt`): altın → stat etiketleri → bileşen ikonları. Etiketler TR'ye çevriliyor (`ITEM_TAG_TR`), listede olmayan etiket sessizce atlanır. **Eşyalar ayrı dosyada** — "Veriyi güncelle" akışı şampiyonları tazeliyor, eşyalar patch'te `build-data.mjs` ile yenilenir. |
| 2026-07-20 | **`Puzzle` ayrık birleşime çevrildi** (`ChampionPuzzle \| ItemPuzzle`, `sub` ayırıcı) | Eşya modunda şampiyon YOK; `champion`'ı isteğe bağlı yapmak her erişimi sessiz `?.`/`!` yığınına çevirirdi. Birleşimle TypeScript 27 kullanım noktasını tek tek gösterdi ve doğru dalı ele almaya zorladı (yanlışlıkla `champion` okunamıyor). Havuz filtresi Eşya'da uygulanmaz — bölge/rol eşyaya anlamsız. Testler için `src/test/helpers.ts` (`champOf`/`asChamp`) eklendi. |
| 2026-07-20 | "Altı Silindir" → **"Tam Takım"**; rozet metinleri ve "Tam Gün" şartı `SUB_MODES.length`'ten türetiliyor | Mod sayısı 6→7 olunca "6 alt mod" metinleri yalan oldu. **Rozet `id`'si DEĞİŞMEDİ** (`six_shooter`) — kazanılmış rozetler `vt:ach`'ta id ile duruyor, ad değişse de kaybolmaz. Üretim kodu zaten `SUB_MODES.every(...)` ile veri güdümlüydü (doğru davrandı); kırılan yalnız 6'yı sabitleyen TESTLERDİ — onlar da mod listesinden türetilecek şekilde yazıldı, bir sonraki modda kendiliğinden uyacak. Yan etki: Günlük'te "Tam Gün" artık 7 mod ister. |

| 2026-07-20 | **Şampiyon bilgi kartı** (`ChampionInfo.tsx`): tur sonu kartında "🔍 … hakkında" → lore + yetenek açıklamaları | Kullanıcı isteği. Lore ve yetenek açıklamaları veride YOKTU; eklendi ama **ana veriye değil**: `src/data/champion-info.json` (272 KB) ayrı dosya ve **dinamik `import()` ile tembel yükleniyor**. Ölçüldü: kendi chunk'ında (296 KB) kalıyor, ana paket şişmiyor. Modül düzeyinde önbelleklenir (her açılışta tekrar çözülmez). ddragon açıklamaları hafif HTML içeriyor → `stripHtml` ile temizleniyor (`<br>` satır sonuna). Eşya modunda buton çıkmaz (şampiyon yok). `nextRound` kartı kapatır, yoksa sonraki şampiyonun kartına dönüşürdü. |

| 2026-07-20 | **Geçmiş günlük cevapları**: takvimde güne tıklayınca o günün cevapları da görünüyor | Kullanıcı isteği. Cevap **kaydediliyor** (`DailyState.answer` → `DailyEntry.a`), tarihten yeniden HESAPLANMIYOR: günlük cevap `fnv1a(tarih) % havuz` ile türüyor, yeni şampiyon eklenince havuz büyür ve geçmiş günlerin hesabı kayar — yeniden hesap tarihi yanlış gösterirdi. `vt:dhistory` değeri düz sayıdan `{g, a?}` nesnesine geçti; **göç kodu YAZILMADI** — `normalizeEntry()` eski düz-sayı kayıtlarını da okuyor (cevabı bilinmez, gerisi çalışır), test edildi. |
| 2026-07-20 | **Paket, değişim hızına göre bölündü** (`manualChunks`): vendor / data-champions / data-items / data-emoji / app | PWA her şeyi önbelleğe aldığı için ilk yükleme zaten tek seferlikti; asıl kayıp **her kod güncellemesinde** oyuncuların şampiyon verisini ve React'ı da yeniden indirmesiydi. Ölçüldü: ana uygulama chunk'ı **478,93 kB → 109,21 kB** (gzip 131,74 → **30,41**). Kod güncellemesinde artık yalnız bu iniyor, veri/vendor önbellekte kalıyor. Üretim derlemesi tarayıcıda açılarak doğrulandı (chunk bölmenin riski başlatma sırası hatasıdır — konsol temiz). |
| 2026-07-20 | Bilgi kartı chunk'ı **precache'ten çıkarıldı** (`globIgnores`), runtime `CacheFirst`'e alındı | Servis çalışanı tüm derleme çıktısını önden indirdiği için 296 kB'lık bilgi verisi tembel yüklemeye rağmen her kurulumda iniyordu. Precache **786 → 497 kB**. Bedeli: kart hiç açılmadan çevrimdışı kalınırsa yüklenemez — `ChampionInfo` bunu kullanıcıya açıkça söylüyor. `maxEntries: 4` ile eski sürümler birikmiyor. |

### Faz 5 — yeni modlar (2026-07-21)

| Tarih | Karar | Gerekçe |
|---|---|---|
| 2026-07-21 | **Silüet modu** (8. alt mod): yükleme ekranı görseli `grayscale + contrast + brightness` ile eşiklenir, her yanlışta gevşer | Renk/doku silinince geriye duruş ve hatlar kalıyor — Görsel modundan farklı bir beceri. **Şeffaf silüet varlığı hiçbir CDN'de yok**, o yüzden CSS filtresi: ek dosya inmiyor, çevrimdışı çalışıyor. Açılma hızı zorluktan: `silhouetteReveals` 3/5/7/10 |
| 2026-07-21 | Silüette otomatik tamamlama listesinde **şampiyon portresi gösterilmez** | Açılır listedeki küçük resim ekrandaki karartılmış görselle karşılaştırılıp cevabı ele veriyordu — modu anlamsızlaştıran sızıntı. `championOptions(withImages)` parametresi eklendi; yalnız silüette kapalı |
| 2026-07-21 | **Hata düzeltmesi:** Eşya modunda yanlış tahmin rozetleri isim yerine ham id gösteriyordu ("3107") | Rozet etiketi `byId()` ile ŞAMPİYON arıyordu; eşya id'si orada bulunmayınca ham anahtar basılıyordu. Kostüm modunda zaten seçenek listesinden okunuyordu — eşya da aynı yola bağlandı |
| 2026-07-21 | Eşya fiyatları **Summoner's Rift** fiyatlarıdır; elle düzeltme tablosu KURULMADI | Kullanıcı Kefaret'i oyunda 2800 görüp bildirdi, bizde 2300 görünüyordu. Üç kaynak (ddragon + CommunityDragon + PBE) 2300 diyordu, stat'lar birebir tutuyordu. **Sebep: ekran görüntüsü Tam Gaz modundan** — özel modlarda eşya ekonomisi ayrı ölçekleniyor. Veri baştan doğruymuş; kısa süre için eklenen `ITEM_GOLD_OVERRIDES` tablosu geri alındı. **Ders: özel mod fiyatıyla karşılaştırıp veriyi "yanlış" sanma.** Patch eşlemesi de netleşti: oyun 26.14 = ddragon 16.14.1 (Riot yıl bazlı numaraya geçti, ddragon eski diziyi sürdürüyor) |
| 2026-07-21 | **Hikâye modu** (9. alt mod, `lore`): şampiyonun biyografisinden bulma; ad metinden silinir (█████), her yanlışta bir cümle daha açılır | Kullanıcının seçtiği üç yeni moddan ikincisi. Veri ZATEN vardı: `champion-info.json` (173/173 lore, şampiyon bilgi kartı için çekilmişti) — yeni indirme yok, aynı tembel parça paylaşılıyor. Başlangıç cümlesi zorluktan: `loreStart` 3/2/1/1. **Kritik güvence testle korunuyor:** 173 şampiyonun hepsinde sansür kontrol ediliyor, ad sızarsa test kırmızı yanar. Günlük'te çıkmaz (`daily: false`) |
| 2026-07-21 | **Havuz filtresi çoklu seçime geçti** (kullanıcı isteği): grup içi VEYA, gruplar arası VE | Tek seçim antrenmanı kısıtlıyordu ("Noxus + Ionia" ya da "Noxus'lu büyücüler" kurulamıyordu). `PoolFilter` artık `{regions, roles, lanes}` dizileri. **Eski anahtarlar okunmaya devam ediyor** (`region:Noxus`) — kayıtlı tercihler ve paylaşılmış meydan okuma linkleri bozulmasın diye; göç kodu gerekmedi. Anahtar sıralı üretiliyor: seçim sırası desteyi bölmesin. Rozetlerdeki sayı artık "bu seçeneğe dokunursam havuz kaç olur" demek. **Test yakaladı:** ön ek kontrolü ilk harfe bakıyordu, `olmayan:Noxus` rol sanılıyordu |
| 2026-07-21 | **`SUB_MODES[].daily` bayrağı + `DAILY_SUBS`** — yeni modlar Günlük'te ÇIKMAZ (kullanıcı kararı: "b" seçeneği) | Her yeni mod Günlük rutinini uzatıyor ve "Tam Gün" eşiğini yükseltiyordu (6→7→8...). Günlük çekirdek 7 modda sabitlendi. Günlük'e bakan HER yer bu listeden besleniyor: menü listesi, `DailyPanel`, `DailyCalendar`, `getFullDayStreak`, `full_day` rozeti, istatistik Günlük sekmesi. Bir modu Günlük'e almak = tek bayrağı silmek |
| 2026-07-21 | **Eşya modu TERS çevrildi** (kullanıcı isteği): ikon artık soru değil, en son açılan ipucu | İkon baştan görününce bilen için bilmece anında bitiyordu. Yeni merdiven: **altın değeri (hep açık)** → statlar → bileşen ikonları → eşya ikonu. `itemGoldAt` alanı `itemIconAt` olarak yeniden adlandırıldı (altın artık koşulsuz görünüyor). Aşırı Zor'da ikon HİÇ açılmaz. Zorluk tablosuna üç eşya satırı eklendi (daha önce hiç yoktu). **İkon açılınca net gelmiyor:** silik + bulanık başlar (`blur 7px`, `opacity .4`), sonraki her yanlışta netleşir — ikon "cevabı veren" değil "yaklaştıran" ipucu olsun diye (kullanıcı isteği) |
| 2026-07-21 | Testlerdeki elle yazılmış mod listeleri `SUB_MODES`/`DAILY_SUBS`'tan türetildi | Silüet eklenince üç test kırıldı (Karışık tip listesi, `full_day`, Tam Gün serisi) — hepsi listeyi elle sayıyordu. Artık yeni mod eklemek testleri kırmıyor, aksine kapsıyor |

## Komutlar

- `npm run dev` — geliştirme (launch.json: "vadi-tahmini")
- `npm test` — oyun mantığı testleri (vitest, tarayıcı gerekmez) · `npm run test:watch` geliştirirken
- `npm run build` — üretim + PWA
- `node scripts/build-data.mjs` — veriyi yeni patch'e güncelle (rapor basar; cinsiyet tablosuna yeni şampiyon eklemeyi unutma)
- `node scripts/emoji-review.mjs` — emoji düzenleme aracını üretir (`tools/emoji-review.html`); yeni şampiyon geldiğinde emojisini buradan ekle

## Bilinen Sınırlar / Faz 2

- Emoji ve Replik modları YAPILDI (2026-07-20) → 3 üst × 6 alt = 18 kombinasyon.
- **İş listesi (2026-07-20) TAMAMLANDI:** zorluk seviyeleri ✅, istatistik ekranı ✅, günlük mod derinleştirme ✅, görsel modu kostüm havuzu ✅. Hepsi kullanıcı testine hazır.
- **Canlı multiplayer ASKIYA ALINDI (kullanıcı kararı, 2026-07-20):** "ben tekrar konusunu açana kadar". Gerekçe: yeni mod eklemek yerine mevcut özellikleri derinleştirmek isteniyor. Kullanıcı açmadan bu konuyu başlatma.
- **Tuzak (yaşandı, düzeltildi):** olay nesnesinin `currentTarget`'ını `setState` güncelleyicisinin İÇİNDE okuma — güncelleyici sonra çalıştığı için `currentTarget` o an `null` olur ve bileşen komple çöker (beyaz ekran). Değeri handler'ın gövdesinde okuyup güncelleyiciye kapat olarak ver. `QuoteView`'daki `onTimeUpdate` bu yüzden patlamıştı.
- Replik modu kullanıcı tarafından denendi: ses çalıyor, kontroller çalışıyor. Kalan riskli noktalar: (1) otomatik çalma tarayıcı politikasına takılabilir — takılırsa buton zaten var, `failed` durumu uyarı basıyor; (2) bazı yasaklanma replikleri şampiyonun adını söylüyor olabilir, oynayınca görülecek; (3) mod kaçınılmaz olarak sesli — sessiz ortamda oynanamaz, bilinçli kabul.
- Emoji kalitesi tur tur düzeltiliyor (ör. Wukong'un 2. emojisi 🪄 → 🦯 asa). Düzeltme yolu: `node scripts/emoji-review.mjs`.
- localStorage anahtarları `lt:` önekli (deste: `lt:deck:*`, istatistik: `lt:stats:*`, günlük: `lt:daily:*`, veri: `lt:data:updated`).
- Yeni şampiyonlar (Locke, Yunara, Zaahen) Meraki'de yokken elle dolduruldu — Meraki eklenince fallback otomatik devre dışı.

## Güncel Durum (2026-07-20)

**MVP tamam ve doğrulandı.** 3 üst × 4 alt mod çalışıyor; tarayıcıda uçtan uca test edildi:
Classic (koridor kolonu dahil), Yetenek (ipuçları), Görsel (zoom açılımı), Kostüm (TR arama "havali"→"Havalı");
Sınırsız (deste, seri sayacı), Günlük (determinizm node+tarayıcı karşılaştırmasıyla doğrulandı: 2026-07-20 Klasik=Jinx; kalıcılık ve "bugün tamamlandı" rozeti çalışıyor), Zamana Karşı (sayaç, skor, Pas, rekor).
Rastgelelik: iki profil simülasyonunda diziler farklı çıktı. Mobil 375px taşma düzeltildi (`w-full` overflow kabı). Üretim build + PWA (manifest/sw 200, ikonlar `scripts/gen-icons.mjs` ile üretildi) hazır.
Pano kopyalama gömülü test tarayıcısında engellendi (NotAllowedError) — çift fallback eklendi, gerçek tarayıcıda test edilmeli.

**Yetenek tuş bonusu eklendi (kullanıcı tarayıcıda doğruladı).** Dokunulan dosyalar:
`GameScreen.tsx` (`slotGuess` state, `awaitingSlot`, `handleSlot`, tuş butonları + sonuç satırı), `PuzzleView.tsx` (`hideSlot` prop artık kullanılıyor; ipucu sırası değişti), `stats.ts` (`DailyState.slot`), `share.ts` (`shareDailySimple` 4. parametre `slotOk`).

**İlk push'a hazır hale getirildi (2026-07-20 akşam):** ad/ikon/klasör/localStorage öneki taşındı, README baştan yazıldı (Vite şablonu duruyordu), `index.html`'e açıklama + Open Graph etiketleri eklendi (WhatsApp/Discord'da link kartı çıksın diye; `og:image` göreli yol — özel alan adı alınırsa mutlak yapmak gerekebilir). `npm run build` + PWA üretimi temiz, `tsc`/`oxlint` temiz.

**UI cilası (2026-07-20, tarayıcıda doğrulandı):** hover/focus durumları, yeni tahmin satırı ve rozetlerinde giriş animasyonu, yanlış tahminde giriş alanı titremesi, kazanma bandı ve bonus panelinde açılma animasyonu, son 10 saniyede sayaç nabzı, mobilde tablo kaydırma ipucu, iPhone için `safe-area-inset-bottom`. 375px ve masaüstünde ölçüldü: sayfa yatay taşması yok, tablo kendi içinde kayıyor.

**Pinterest turu (2026-07-20):** kullanıcı 4 referans verdi (quiz şablonu, Wordi kelime oyunu, "magic nav menu", Wordle Thanksgiving). Alınanlar: nötr "yanlış" rengi + üst çubuk ayracı (Wordle), how-to-play ekranı + küçük etiket/büyük rakam skor (Wordi), üstten radyal aydınlanmayla derinlik (quiz şablonu). ALINMAYAN: alt navigasyon çubuğu (3 ekranlık uygulamaya gereksiz), çizgi film/rozet süslemeleri (LoL'ün ciddi altın diliyle çakışıyor).

Not: `.claude/launch.json`'da `autoPort: true` var (paralel oturumda 5173 doluydu); Vite PORT env'ini yok sayıp 5174'e düşüyor, tarayıcı panelinde adres elle girilmeli.

**Zamana Karşı × tuş bonusu düzeltmeleri (2026-07-20, ikinci oturum):** Enter ile mid-run ilerleme tarayıcıda doğrulandı (band kapandı, sayaç sıfırlanmadı, yeni soru geldi); süre-dolarken-tuş-bekliyor telafisi de canlı testle doğrulandı. `tsc`/`oxlint`/36 test yeşil.

**YAYINDA (2026-07-20):** https://vadi-tahmini.vercel.app — GitHub `YusufEnesYilmaz/vadi-tahmini` (main) Vercel'e bağlı, push = otomatik deploy. Güvenlik başlıkları canlıda doğrulandı (CSP, nosniff, X-Frame-Options, HSTS). `og:image` mutlak adrese çevrildi.

**Rozet sayım hatası düzeltildi (2026-07-20, kod incelemesi):** `buildSnapshot` Günlük kayıtlarını 4× sayıyor ve Aşırı Zor/Zor rozetlerini Günlük oynayınca açıyordu (ayrıntı karar günlüğünde). Ayrıca rozet tarihi UTC yerine yerel `todayKey()`'e alındı, `checkAchievements` bağımlılığı eklenerek son lint uyarısı da temizlendi. **87 test / `tsc` / `oxlint` tamamen temiz.**
⚠ Not: Hata düzelmeden önce yanlışlıkla açılmış rozetler `vt:ach`'ta kayıtlı kalır (depo bir kez yazınca geri almıyor). Temiz başlangıç isteyen Ayarlar > "Tüm ilerlemeyi sıfırla" kullanabilir; canlıdaki oyuncular için geri alma kodu yazılmadı — arkadaş grubu ölçeğinde gereksiz görüldü.

**Tur sonu kartı (2026-07-20, kullanıcı isteği):** Ayrı kazanma/kaybetme bantları tek karta birleşti — durum başlığı ("🎉 Bildin!" / "😔 Bulamadın"), **şampiyonun görseli** (kostüm/görsel modunda sorunun görseli), cevabın adı, deneme sayısı, varsa tuş bonusu sonucu, altta "Sonraki →". İki bandın kopya paylaşım kodu da tekilleşti.
**Kazara atlama düzeltildi:** Enter ile tahmin seçilirken klavye tekrarı (`e.repeat`) veya alışkanlıkla atılan ikinci Enter, tur biter bitmez input kaybolduğu için sonuç kartını anında geçiriyordu. İki koruma: `e.repeat` yok sayılıyor + tur bitiminden sonra 800 ms Enter kilidi (`finishedAtRef`).
**Günlük Klasik sabitlendi:** `DAILY_OVERRIDES` mevcut mekanizmasına `classic?: string` alanı eklendi; **2026-07-20 ve 2026-07-21** için `classic: 'Seraphine'` (kullanıcı isteği). Kaldırmak için o satırı silmek yeterli — tarih geçince zaten etkisizleşir. Yanlış id yazılırsa sessizce normale döner.
⚠ Test ederken: bugünün Günlük Klasik'i daha önce oynandıysa `vt:daily:classic` kaydı "tamamlandı" der. Yeniden oynamak için Ayarlar > "Tüm ilerlemeyi sıfırla" ya da o anahtarı silmek gerekir.

**Faz 4 tamamlandı (2026-07-20, altı madde sırayla):** ① ilerleme yedekleme (Ayarlar'da al/yükle), ② havuz filtreleri (bölge/rol/koridor), ③ **Eşya modu** (7. alt mod, 143 eşya), ④ şampiyon bilgi kartı (lore + yetenekler, tembel yüklenen 296 kB), ⑤ geçmiş günlük cevapları (takvimde), ⑥ paket bölme (ana chunk 478→109 kB).
Yan etki bilinmeli: mod sayısı 6→7 olduğu için Günlük'te "Tam Gün" artık 7 mod istiyor, "Altı Silindir" rozeti "Tam Takım" adını aldı (id değişmedi, kazanılmışlar duruyor).
**106 test / `tsc` / `oxlint` / üretim derlemesi temiz; üretim derlemesi tarayıcıda konsol hatasız açıldı.** Oyun içi test kullanıcıda.

Sıradaki aday işler: Faz 2'den kalanlar veya yeni istekler. Masaüstü `.exe` konuşuldu: önce PWA denenecek, gerekirse Tauri sarmalayıcısı (kod değişmez).
